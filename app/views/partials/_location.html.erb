<div id="location" data-meetle-id="<%= @meetle.id %>">
  <% @meetle.locations.each do |location| %>
    <h2>  <%= location.user.name %>'s location: <%= location.station.name %></h2>
  <% end %>
</div>
